---
import { markdownHTML } from "@/web/utils/parsers";

interface Props {
  content: string;
}

const { content } = Astro.props;
const html = await markdownHTML(content);
---

<div set:html={html} />
